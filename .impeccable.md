# Launchpad — 设计上下文 (Impeccable)

> 由 Impeccable 技能生成并维护。修改 UI 前先读此文件。

## 产品定位
- **是什么**：Windows 桌面级极简应用启动器（Tauri 2 + React 19 + Vite + TS/Rust）。
- **目标用户**：像"张总"这样的重度桌面用户——每天开很多软件、想要一个干净统一的工作入口。
- **使用情境**：常驻桌面、置顶或边缘磁吸；快速启动应用、看天气/日历/待办/剪贴板/网速等小组件。

## 品牌个性 / Tone
- 关键词：**克制、安静、精致、可信赖**。像一张干净的木纹书桌，而不是花哨的游戏启动器。
- 不追求"酷炫发光"，追求"每次打开都舒服"。

## 设计方向（已与用户确认）
- **风格**：精致极简（Refined Minimalism）——靠留白、字重、层次与节奏制造高级感，而非靠装饰。
- **强调色**：青绿 teal（非蓝、非紫）。
  - 浅色 `--accent: 13 148 136`（teal-600），`--accent-soft: rgba(13,148,136,0.10)`
  - 深色 `--accent: 45 212 191`（teal-400），`--accent-soft: rgba(45,212,191,0.14)`
- **中性色**：温暖中性（paper 偏米白 `250 249 247`，ink 近黑 `26 26 26`），非纯黑纯白。

## 设计系统（已落地）
- 语义 token（见 `src/styles.css` / `tailwind.config.js`）：`ink`(文字层级), `paper`(背景), `line`(分割), `accent`(强调)。**所有强调色必须走 `accent` token，禁止写死蓝/绿/紫**。
- 圆角：卡片 `--radius-lg: 16px`；小组件 `rounded-2xl`。
- 字体：正文系统无衬线；标题 `font-display`（LXGW WenKai 衬线）；标签 `label-mono`（等宽大写小字）。
- 卡片：`.card-shadow` / `.card-shadow-hover`（悬停微强调环用 accent）。
- 动效：状态变化用 `ease-out` 缓动（cubic-bezier(0.2,0.8,0.2,1)）；仪表盘组件错落入场（`.dashboard-grid > *` nth-child 延迟）。

## 明确保留的"非品牌"色（不要动）
- 平台品牌色（微博红、知乎蓝等）——`Sidebar` / `TrendingView` 中真实品牌识别色。
- 数据可视化色（内存 emerald、磁盘 violet、番茄钟状态色）——用于区分数据系列，多色更好。
- AppTile 的图标兜底渐变——装饰性多彩，区分应用。

## 禁忌
- 不要引入纯黑 `#000` / 纯白 `#fff` 作为主背景。
- 不要做毛玻璃滥用、无意义火花线、圆角+通用阴影的"AI 味"堆砌。
- 不要回退到蓝色品牌色。
